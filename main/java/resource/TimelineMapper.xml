<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="PMS.dao.TimelineDao">

	<!-- Timeline List -->
	<select id="getTimeline" parameterType="int" resultType="Timeline">
	SELECT s.pno,s.SNAME,c.CNO ,c.WRITER,c.REGDTE, c.sno,c.updte
	FROM comment_tb c FULL OUTER JOIN SCHEDULE s ON (s.sno=c.sno)
	WHERE s.pno= #{pno}
	</select>

</mapper>

