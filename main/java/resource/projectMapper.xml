<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="PMS.dao.ProjectDao">
	<select id="showMyProject" resultType="mySchedule" parameterType="string">
		SELECT p.USERNO, pj.TITLE, pj.DEPT, s.*
		FROM SCHEDULE s, PROJECT pj, PARTICIPANT p
		WHERE pj.PNO = s.PNO AND pj.PNO = p.PNO AND s.SNO = p.SNO
		AND p.USERNO = #{userno}
		ORDER BY s.SNO
	</select>
</mapper>


   